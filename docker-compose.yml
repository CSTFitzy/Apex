services:
  postgres:
    image: postgis/postgis:16-3.3
    environment:
      POSTGRES_DB: sharknet
      POSTGRES_USER: sharknet_user
      POSTGRES_PASSWORD: sharknet_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - sharknet-network

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - sharknet-network

  backend:
    build:
      context: .
      dockerfile: server/Dockerfile
    environment:
      - NODE_ENV=development
      - DB_HOST=postgres
      - REDIS_HOST=redis
    ports:
      - "3000:3000"
    depends_on:
      - postgres
      - redis
    volumes:
      - ./server:/app/server
    networks:
      - sharknet-network

  frontend:
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app/client
    networks:
      - sharknet-network

volumes:
  postgres_data:

networks:
  sharknet-network:
    driver: bridge
